# Live season data — never commit; contains real rosters and game logs
/data/
/data-test/
_backups/

# Real HUDL / MaxPreps exports contain real students' names and stats.
# Matched anywhere, not just at the repo root: the working copy keeps them in a
# "Hudl Files/" subfolder, which the old root-anchored patterns walked straight
# past. test/fixtures/ is excluded from these rules and stays committed.
Hudl Files/
Rosters/
HUDL SEASON FILES/
HUDL*.txt
HUDL*.html
MAXPREPS*.pdf
!test/fixtures/**

# local tooling config
.claude/

node_modules/
.DS_Store
*.tmp
